// @flow
const prefix = 'papagaio';

export const home = `${prefix}.home`;
export const about = `${prefix}.about`;
export const another = `${prefix}.another`;
export const filters = `${prefix}.filters`;
export const userInfo = `${prefix}.user-info`;
